## 2024-05-24 - Missing Input Validation in Invitation API | **Learning:** API routes accepting POST/PUT requests for creating/updating invitations were directly reading from request bodies, exposing the application to XSS vulnerabilities via un-sanitized fields like title and description. | **Prevention:** Always pipe incoming request payloads through established validation functions like `validateInvitationData` and strictly use `validation.sanitizedData` before persisting to the database.
## 2026-03-08 - Missing Authentication on Template API Routes | **Learning:** The `POST`, `PUT`, and `DELETE` methods on `/api/templates` and `/api/templates/[id]` were missing `getServerSession` checks, despite comments indicating they were admin-only. This allowed any unauthenticated user to modify or delete default templates, bypassing intended authorization. | **Prevention:** Always ensure that any API route performing state-mutating operations (POST, PUT, DELETE) explicitly checks for a valid session using `getServerSession` before proceeding with database operations, especially when using the privileged `supabaseAdmin` client.
## 2026-03-12 - Insecure JSON-LD Injection in Script Tags | **Learning:** Using `JSON.stringify` directly within `dangerouslySetInnerHTML` for `<script type="application/ld+json">` is vulnerable to XSS if user-controlled strings contain `</script>`, as the browser terminates the script block regardless of string delimiters. | **Prevention:** Use a dedicated serialization utility like `serializeJsonLd` that escapes `<`, `>`, and `&` to Unicode sequences (`\u003c`, `\u003e`, `\u0026`) before injecting into HTML.
## 2024-03-14 - Direct API Supabase Client RLS Bypass | **Learning:** Found that using `supabase.from('designs').insert(...)` directly in `src/app/api/upload/route.ts` bypasses the centralized Data Access Layer (`src/lib/database-supabase.ts`) which properly enforces RLS rules securely using `supabaseAdmin`. Storage file modifications directly in API routes using `supabase.storage` can also violate architectural constraints. | **Prevention:** Always delegate database and storage interactions to methods in `supabaseDb` (in `src/lib/database-supabase.ts`) utilizing `supabaseAdmin` for safe operations, and ensure that all user inputs (such as file names used as design names) are sanitized with `sanitizeText` before they are sent to the database.
## 2026-03-17 - Secure Data Access for Guest RSVPs | **Learning:** Writing guest RSVPs directly to Supabase via the anonymous client from `src/app/api/rsvp` violates strict API architecture rules and will fail due to write RLS. | **Prevention:** All data access bypassing write RLS must securely leverage the service role (`supabaseAdmin`) via `src/lib/database-supabase.ts` and utilize strict backend validation (e.g. `validateRSVPData`) to enforce schema rules.
## 2024-03-18 - Direct Supabase Storage Access in API Routes | **Learning:** Direct interactions with `supabase.storage` in `src/app/api/*` routes violate architectural constraints, as all storage and database logic should be cleanly abstracted in the Data Access Layer (`src/lib/database-supabase.ts`).  | **Prevention:** Always delegate direct Supabase client calls (including `.storage`) to centralized DAO methods utilizing `supabaseAdmin` within `src/lib/database-supabase.ts`.
## 2026-03-20 - Arbitrary File Deletion via Path Traversal | **Learning:** Parsing user-controlled object URLs (like `image_url`) and passing the extracted filepath to a privileged API (like `supabaseAdmin.storage.remove`) without path validation creates an IDOR vulnerability, allowing attackers to delete arbitrary files using directory traversal (`../`). | **Prevention:** Always decode and strictly validate extracted filepaths (e.g., verifying `userId/filename` format and rejecting `..` or slashes in filenames) before executing backend deletion operations on storage.
## 2026-03-30 - Unauthenticated External API Calls in Next.js Routes | **Learning:** Always check Next.js App Router API endpoints that call external services (like Gemini) to ensure they are protected by `getServerSession`, even if part of the route is meant for unauthenticated demo access. | **Prevention:** Manually verify `getServerSession` at the top of any server-side logic that interacts with external APIs or databases (unless explicitly public like `/api/rsvp`), before the sensitive operation occurs.
## 2026-04-06 - Missing share_token validation in RSVP endpoint | **Learning:** Guest access relies on `share_token` to bypass session checks safely. However, the `POST /api/rsvp` endpoint previously only validated the `invitation_id` provided in the body, which allowed an IDOR (Insecure Direct Object Reference) attack where anyone with an `invitation_id` could spam RSVPs. | **Prevention:** To prevent IDOR vulnerabilities in link-based guest access routes, validate operations using the secret `share_token` alongside the public identifier (e.g., `invitation_id`). Ensure both frontend hooks and tests are updated to provide this token.
## 2024-05-19 - Prevent HTML Injection in Email Templates
**Learning:** Interpolating user-controlled data directly into HTML string templates (e.g., Resend email generation) creates a vector for XSS and HTML injection, even if the input was previously sanitized for general storage. Generic sanitization (`DOMPurify` without strict `ALLOWED_TAGS` or custom regex replacements) might strip `<script>` but allow `<img>` or other harmful tags.
**Prevention:** Always explicitly escape user inputs using an `escapeHTML` utility right before interpolation into HTML templates to ensure safe rendering of user data within emails.
## 2026-04-20 - Insecure Guest RSVP Write Access | **Learning:** Writing guest RSVPs directly to Supabase via the anonymous client from `src/app/api/rsvp` violates strict API architecture rules and will fail due to write RLS. | **Prevention:** All data access bypassing write RLS must securely leverage the service role (`supabaseAdmin`) via `src/lib/database-supabase.ts` and utilize strict backend validation (e.g. `validateRSVPData`) to enforce schema rules.
## 2026-04-26 - Complete Isolation of Privileged Operations | **Learning:** Allowing `supabaseAdmin` calls directly in API routes (like cron jobs or deletion routes) leads to architectural drift and potential security gaps where RLS-bypasses are not properly audited. | **Prevention:** Strictly enforce the repository pattern by centralizing ALL `supabaseAdmin` database and storage interactions within `src/lib/database-supabase.ts`. API routes must only interact with the database through this unified facade, ensuring that all privileged operations are consistently implemented and tested.
## 2025-04-27 - Fix XSS Vulnerability in Design Name | **Learning:** User inputs from POST/PUT bodies that are eventually rendered must be sanitized. The `name` field in designs was directly saved to the DB without sanitization. | **Prevention:** Always use `sanitizeText` from `src/lib/security.ts` on text inputs before passing them to the Supabase Data Access Layer.
## 2026-05-04 - Enforce Admin Authorization on Template Routes | **Learning:** The /api/templates routes lacked proper admin role verification, relying solely on whether a user was authenticated (IDOR vulnerability). | **Prevention:** Always explicitly verify admin privileges using a reliable source of truth (like an ADMIN_EMAILS environment variable) for routes that modify global application data, ensuring emails are properly normalized (trimmed and lowercased) before comparison.
